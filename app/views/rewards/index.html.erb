<h1>Listing rewards</h1>

<table>
  <tr>
    <th>Program</th>
    <th>Category</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @rewards.each do |reward| %>
  <tr>
    <td><%= reward.program.name %></td>
    <td><%= reward.category.name %></td>
    <td><%= link_to 'Show', reward %></td>
    <td><%= link_to 'Edit', edit_reward_path(reward) %></td>
    <td><%= link_to 'Destroy', reward, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Reward', new_reward_path %>
<%= link_to "Issuers", issuers_path %>
<%= link_to "Cards", cards_path %>
<%= link_to "Programs", programs_path %>
<%= link_to "Categories", categories_path %>
